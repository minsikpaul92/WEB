<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Climate Solutions</title>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body class="min-h-screen bg-base-200 text-base-content">
    <%- include('partials/navbar') %>

    <main class="mx-auto max-w-6xl space-y-12 px-6 py-16">
      <section class="rounded-3xl bg-base-300 p-10 text-center shadow-2xl">
        <h1 class="mt-4 text-4xl font-bold text-white">Welcome</h1>
        <p class="mx-auto mt-3 max-w-2xl text-base text-white/80">
          Browse our current collection of modern solutions to help fight Climate Change.
        </p>
        <div class="mt-8 flex flex-wrap justify-center gap-4">
          <a href="/solutions/projects" class="btn bg-success text-gray-900">
            Get Started Now
          </a>
        </div>
      </section>

      <section class="space-y-6">
        <div class="flex items-center justify-between">
          <div>

            <h2 class="text-2xl font-semibold text-white">
              Browse a few highlights from the library
            </h2>
          </div>
          <a href="/solutions/projects" class="link link-hover text-[#d3d3d3]">
            Browse all →
          </a>
        </div>

        <% if (featuredProjects && featuredProjects.length) { %>
        <div class="grid grid-cols-1 gap-6">
          <% featuredProjects.forEach((project) => { %>
          <article
            class="flex flex-col overflow-hidden rounded-3xl border border-base-200 bg-base-100 shadow-2xl"
          >
            <div class="h-64 w-full overflow-hidden">
              <img
                src="<%= project.feature_img_url %>"
                alt="<%= project.title %> preview image"
                class="h-full w-full object-cover"
              >
            </div>
            <div class="flex flex-1 flex-col gap-4 p-8">

              <h3 class="text-xl font-semibold text-gray-900"><%= project.title %></h3>
              <p class="text-sm leading-relaxed text-base-content/70">
                <%= project.summary_short %>
              </p>
              <div class="mt-auto">
                <a
                  href="/solutions/projects/<%= project.id %>"
                  class="btn btn-sm bg-success text-gray-900"
                >
                  More information
                </a>
              </div>
            </div>
          </article>
          <% }) %>
        </div>
        <% } else { %>
        <p class="text-base text-base-content/60">
          No featured projects yet. Check the full projects catalogue while we set
          things up.
        </p>
        <% } %>
      </section>
    </main>
  </body>
</html>
