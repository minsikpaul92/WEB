<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects • Climate Solutions</title>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body class="min-h-screen bg-base-100 text-base-content">
    <%- include('partials/navbar') %>

    <main class="mx-auto flex max-w-6xl flex-col gap-10 px-6 py-16">
      <header class="space-y-3">
        <p class="text-sm uppercase tracking-wide text-[#d3d3d3]">Solutions</p>
        <h1 class="text-3xl font-semibold">Projects by sector</h1>
        <% if (sector) { %>
        <p class="text-base text-base-content/70">
          Showing projects that match the
          <span class="font-semibold"><%= sector %></span> sector.
        </p>
        <a href="/solutions/projects" class="btn btn-sm bg-success text-gray-900 w-fit">
          Browse all projects
        </a>
        <% } else { %>
        <p class="text-base text-base-content/70">
          Filter by sector to discover tailored initiatives or browse everything
          below.
        </p>
        <% if (session.user) { %>
          <a href="/solutions/addProject" class="btn btn-success btn-outline btn-sm mt-4 normal-case">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4 mr-1">
              <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
            </svg>
            Add New Project
          </a>
        <% } %>
        <% } %>
      </header>

      <% if (error) { %>
      <div class="alert alert-error bg-error/10 text-error">
        <div>
          <span><%= error %></span>
          <% if (validSectors && validSectors.length) { %>
          <span>
            Available sectors:
            <%= validSectors.join(", ") %>
          </span>
          <% } %>
        </div>
      </div>
      <% } %>

      <% if (projects && projects.length) { %>
      <div class="overflow-x-auto rounded-2xl border border-base-200 shadow-sm">
        <table class="table">
          <thead>
            <tr>
              <th>Title</th>
              <th>Sector</th>
              <th>Summary</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            <% projects.forEach((project) => { %>
            <tr>
              <td class="font-semibold"><%= project.title %></td>
              <td>
                    <a
                      href="/solutions/projects?sector=<%= encodeURIComponent(project.Sector.sector_name) %>"
                      class="text-xs uppercase font-bold text-gray-500 hover:text-gray-700"
                    >
                      <%= project.Sector.sector_name %>
                    </a>
              </td>
              <td class="text-sm text-base-content/70">
                <%= project.summary_short %>
              </td>
              <td>
                <a
                  href="/solutions/projects/<%= project.id %>"
                  class="btn btn-sm bg-success text-gray-900"
                >
                  View details
                </a>
              </td>
            </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
      <% } else if (!error) { %>
      <p class="text-base text-base-content/70">
        No projects to display. Try selecting a different sector.
      </p>
      <% } %>
    </main>
  </body>
</html>
